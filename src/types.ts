export enum GameRuleType {
    Boolean = "boolean",
    Integer = "integer",
}

export enum GameMode {
    Survival = "survival",
    Creative = "creative",
    Spectator = "spectator",
    Adventure = "adventure",
}

export enum Difficulty {
    Peaceful = "peaceful",
    Easy = "easy",
    Normal = "normal",
    Hard = "hard",
}